# Worker - Arquitetura Visual

## Fluxo de Execução

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            WORKER PROCESS                                │
└─────────────────────────────────────────────────────────────────────────┘

1. STARTUP
   ┌─────────────┐
   │ cmd/main.go │ ./bin/financial worker
   └──────┬──────┘
          │
          v
   ┌──────────────────┐
   │ cmd/worker/      │ 1. Load Config
   │ worker.go        │ 2. Initialize O11y (OpenTelemetry)
   │                  │ 3. Connect Database
   │ Run()            │ 4. Connect RabbitMQ
   └──────┬───────────┘ 5. Create Scheduler
          │             6. Register Jobs
          │             7. Start Scheduler
          v
   ┌──────────────────┐
   │ pkg/scheduler/   │
   │ scheduler.go     │ - Creates cron instance
   │                  │ - Wraps jobs with middleware
   │ New()            │ - Enables recovery
   │ Register()       │ - Starts cron
   │ Start()          │
   └──────┬───────────┘
          │
          v
   ┌──────────────────────────────────────────────┐
   │         REGISTERED JOBS (Examples)            │
   ├──────────────────────────────────────────────┤
   │                                               │
   │  ┌─────────────────────────────────────┐     │
   │  │ DatabaseCleanupJob                  │     │
   │  │ Schedule: "0 2 * * *"              │     │
   │  │ Run: Delete old soft-deleted rows   │     │
   │  └─────────────────────────────────────┘     │
   │                                               │
   │  ┌─────────────────────────────────────┐     │
   │  │ ReportGeneratorJob                  │     │
   │  │ Schedule: "0 8 * * 1"              │     │
   │  │ Run: Generate report + Publish MQ   │     │
   │  └─────────────────────────────────────┘     │
   │                                               │
   └───────────────────────────────────────────────┘

2. RUNTIME
   ┌────────────────────────────────────────────────────────────┐
   │                   SCHEDULER LOOP                           │
   │                                                             │
   │  Timer Tick → Match Schedule → Execute Job Wrapper         │
   │                                        │                    │
   │                                        v                    │
   │                            ┌─────────────────────┐         │
   │                            │  Job Wrapper        │         │
   │                            ├─────────────────────┤         │
   │                            │ 1. Check shutdown   │         │
   │                            │ 2. Check concurrent │         │
   │                            │ 3. Create timeout   │         │
   │                            │ 4. Recovery setup   │         │
   │                            │ 5. Log start        │         │
   │                            │ 6. ► job.Run(ctx)   │         │
   │                            │ 7. Log end/error    │         │
   │                            └──────────┬──────────┘         │
   │                                       │                    │
   │                                       v                    │
   │                            ┌─────────────────────┐         │
   │                            │  Actual Job Logic   │         │
   │                            ├─────────────────────┤         │
   │                            │ - Query Database    │         │
   │                            │ - Process Data      │         │
   │                            │ - Publish RabbitMQ  │         │
   │                            │ - Write Results     │         │
   │                            └─────────────────────┘         │
   └────────────────────────────────────────────────────────────┘

3. SHUTDOWN (Graceful)
   ┌────────────────┐
   │ SIGINT/SIGTERM │  Ctrl+C or kill signal
   └───────┬────────┘
           │
           v
   ┌──────────────────────────────────────────────────────┐
   │            SHUTDOWN SEQUENCE (30s timeout)            │
   ├──────────────────────────────────────────────────────┤
   │                                                       │
   │  1. Signal caught in main context                    │
   │     ├─ Stop accepting new job executions             │
   │     └─ Cancel all job contexts                       │
   │                                                       │
   │  2. scheduler.Shutdown(ctx)                          │
   │     ├─ Stop cron scheduler                           │
   │     ├─ Wait for running jobs (WaitGroup)             │
   │     └─ Return when all jobs finish or timeout        │
   │                                                       │
   │  3. rabbitClient.Shutdown(ctx)                       │
   │     ├─ Close channels                                │
   │     └─ Close connection                              │
   │                                                       │
   │  4. dbManager.Shutdown(ctx)                          │
   │     ├─ Close prepared statements                     │
   │     └─ Close connection pool                         │
   │                                                       │
   │  5. o11y.Shutdown(ctx)                               │
   │     ├─ Flush pending traces                          │
   │     ├─ Flush pending metrics                         │
   │     └─ Close exporters                               │
   │                                                       │
   └──────────────────────────────────────────────────────┘
```

## Componentes e Responsabilidades

```
┌─────────────────────────────────────────────────────────────────┐
│                        COMPONENT DIAGRAM                         │
└─────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────┐
│                         pkg/jobs/                                 │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  type Job interface {                                   │     │
│  │      Run(ctx context.Context) error                     │     │
│  │      Name() string                                      │     │
│  │      Schedule() string                                  │     │
│  │  }                                                       │     │
│  │                                                          │     │
│  │  type Config struct {                                   │     │
│  │      DefaultTimeout    time.Duration                    │     │
│  │      EnableRecovery    bool                             │     │
│  │      MaxConcurrentJobs int                              │     │
│  │  }                                                       │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                   │
│  📋 Responsabilidade: Define contratos e configurações           │
└───────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────┐
│                      pkg/scheduler/                               │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  type Scheduler struct {                                │     │
│  │      cron   *cron.Cron                                  │     │
│  │      o11y   observability.Observability                 │     │
│  │      config *jobs.Config                                │     │
│  │      jobs   []jobs.Job                                  │     │
│  │      ctx    context.Context                             │     │
│  │      cancel context.CancelFunc                          │     │
│  │      wg     sync.WaitGroup                              │     │
│  │  }                                                       │     │
│  │                                                          │     │
│  │  func New(ctx, o11y, config) *Scheduler                 │     │
│  │  func (s *Scheduler) Register(job) error                │     │
│  │  func (s *Scheduler) Start()                            │     │
│  │  func (s *Scheduler) Shutdown(ctx) error                │     │
│  │  func (s *Scheduler) wrapJob(job) func()               │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                   │
│  📋 Responsabilidade:                                            │
│     - Gerenciar ciclo de vida de jobs                            │
│     - Adicionar middleware (timeout, recovery, logging)          │
│     - Controlar concorrência                                     │
│     - Graceful shutdown                                          │
└───────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────┐
│                  internal/worker/jobs/                            │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  DatabaseCleanupJob                                     │     │
│  │  ├─ db: *sql.DB                                         │     │
│  │  ├─ o11y: Observability                                 │     │
│  │  ├─ Name() "database_cleanup"                           │     │
│  │  ├─ Schedule() "0 2 * * *"                              │     │
│  │  └─ Run(ctx) → DELETE old records                       │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  ReportGeneratorJob                                     │     │
│  │  ├─ db: *sql.DB                                         │     │
│  │  ├─ publisher: *rabbitmq.Publisher                      │     │
│  │  ├─ exchange: string                                    │     │
│  │  ├─ o11y: Observability                                 │     │
│  │  ├─ Name() "report_generator"                           │     │
│  │  ├─ Schedule() "0 8 * * 1"                              │     │
│  │  └─ Run(ctx) → SELECT data → Publish MQ                 │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                   │
│  📋 Responsabilidade: Implementações específicas de jobs         │
└───────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────┐
│                     cmd/worker/worker.go                          │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  func Run() error                                       │     │
│  │                                                          │     │
│  │  1. LoadConfig()                                        │     │
│  │  2. NewProvider(o11yConfig) → o11y                      │     │
│  │  3. postgres.New(dsn) → dbManager                       │     │
│  │  4. rabbitmq.New(o11y) → rabbitClient                   │     │
│  │  5. scheduler.New(ctx, o11y, jobConfig) → sched         │     │
│  │  6. sched.Register(jobs...)                             │     │
│  │  7. sched.Start()                                       │     │
│  │  8. <-ctx.Done() // Wait for signal                     │     │
│  │  9. Shutdown sequence                                   │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                   │
│  📋 Responsabilidade:                                            │
│     - Entry point do worker                                      │
│     - Wiring de dependências                                     │
│     - Coordenação de shutdown                                    │
└───────────────────────────────────────────────────────────────────┘
```

## Fluxo de Dados

```
┌──────────────────────────────────────────────────────────────┐
│                    DATA FLOW DIAGRAM                          │
└──────────────────────────────────────────────────────────────┘

DatabaseCleanupJob:
┌──────┐      ┌──────────┐      ┌──────────┐
│ Cron │ ───▶ │   Job    │ ───▶ │ Database │
│Timer │      │  Logic   │      │          │
└──────┘      └──────────┘      └──────────┘
              Query old          DELETE
              records            records
                │
                v
           ┌──────────┐
           │   Logs   │
           │   O11y   │
           └──────────┘

ReportGeneratorJob:
┌──────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
│ Cron │ ───▶ │   Job    │ ───▶ │ Database │ ───▶ │ RabbitMQ │
│Timer │      │  Logic   │      │          │      │          │
└──────┘      └──────────┘      └──────────┘      └──────────┘
              1. Collect         2. Serialize     3. Publish
                 stats              JSON            event
                   │
                   v
              ┌──────────┐
              │   Logs   │
              │   O11y   │
              └──────────┘
```

## Lifecycle Estados

```
┌────────────────────────────────────────────────────────────┐
│                   LIFECYCLE STATES                          │
└────────────────────────────────────────────────────────────┘

┌──────────────┐
│ INITIALIZING │  Loading config, connecting dependencies
└──────┬───────┘
       │
       v
┌──────────────┐
│  REGISTERING │  Registering jobs in scheduler
└──────┬───────┘
       │
       v
┌──────────────┐
│   RUNNING    │  Scheduler active, jobs executing on schedule
└──────┬───────┘
       │
       │  (SIGINT/SIGTERM received)
       v
┌──────────────┐
│  SHUTTING    │  No new executions, waiting for active jobs
│    DOWN      │  (max 30s)
└──────┬───────┘
       │
       v
┌──────────────┐
│  TERMINATED  │  All resources released, process exits
└──────────────┘

Job Execution States:
┌─────────┐     ┌─────────┐     ┌──────────┐     ┌──────────┐
│ PENDING │ ──▶ │ RUNNING │ ──▶ │ SUCCESS  │ or  │  FAILED  │
└─────────┘     └─────────┘     └──────────┘     └──────────┘
                     │
                     │ (panic)
                     v
                ┌──────────┐
                │ RECOVERED│
                └──────────┘
```

## Dependências Externas

```
┌────────────────────────────────────────────────────────────┐
│                  EXTERNAL DEPENDENCIES                      │
└────────────────────────────────────────────────────────────┘

Worker Process
     │
     ├─ Database (PostgreSQL/CockroachDB)
     │   ├─ Read data for jobs
     │   ├─ Write results
     │   └─ Cleanup operations
     │
     ├─ RabbitMQ
     │   ├─ Publish events
     │   └─ Notify external systems
     │
     ├─ OpenTelemetry Collector
     │   ├─ Send traces
     │   ├─ Send metrics
     │   └─ Send logs
     │
     └─ Configuration (.env)
         ├─ Database DSN
         ├─ RabbitMQ URL
         ├─ OTEL endpoint
         └─ Worker settings
```

## Concurrency Model

```
┌────────────────────────────────────────────────────────────┐
│               CONCURRENCY & SYNCHRONIZATION                 │
└────────────────────────────────────────────────────────────┘

Scheduler (main goroutine)
     │
     ├─ Cron Goroutine (internal to robfig/cron)
     │   └─ Spawns job wrappers at scheduled times
     │
     ├─ Job Wrapper Goroutines (multiple)
     │   ├─ Job 1 Execution ───┐
     │   ├─ Job 2 Execution ───┼─ WaitGroup
     │   └─ Job N Execution ───┘
     │
     └─ Shutdown Coordinator
         ├─ context.Cancel() → Signal all jobs
         ├─ cron.Stop() → No new executions
         └─ wg.Wait() → Await active jobs

Synchronization Primitives:
- context.Context: Cancellation signal
- sync.WaitGroup: Track active jobs
- sync.Mutex: Protect concurrent counter
- Channel: Signal completion
```

---

**Legenda:**
- `┌─┐ └─┘` = Componentes
- `─▶` = Fluxo de dados/controle
- `│ ├ └` = Hierarquia/Árvore
- `→` = Transformação/Ação
